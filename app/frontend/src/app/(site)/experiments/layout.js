// app/experiments/layout.js
export const metadata = {
  title: 'Experiments — ML-Agents Lab',
};

export default function ExperimentsLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}
